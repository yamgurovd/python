"""Кремниевая долина 🌶️🌶️
Искусственный интеллект Антон, созданный Гилфойлом, взломал сеть умных холодильников.
Теперь он использует их в качестве серверов "Пегого дудочника".
Помогите владельцу фирмы отыскать все зараженные холодильники.

Для каждого холодильника существует строка с данными, состоящая из строчных букв и цифр,
и если в ней присутствует слово "anton" (необязательно рядом стоящие буквы, главное наличие последовательности букв),
то холодильник заражен и нужно вывести номер холодильника, нумерация начинается с единицы.

Формат входных данных
В первой строке подаётся число n – количество холодильников. В последующих n строках вводятся строки,
содержащие латинские строчные буквы и цифры, в каждой строке от 5 до 100 символов.

Формат выходных данных
Программа должна вывести номера зараженных холодильников через пробел. Если таких холодильников нет,
ничего выводить не нужно."""


# Первый способ решения задачи - взято из Chat GPT
def find_infected_refrigerators():
    n = int(input())  # Читаем количество холодильников
    infected_indices = []  # Список для хранения номеров зараженных холодильников

    for i in range(n):
        refrigerator_data = input()  # Читаем строку с данными холодильника
        # Проверяем наличие последовательности букв "anton"
        if contains_anton(refrigerator_data):
            infected_indices.append(i + 1)  # Добавляем номер холодильника (начиная с 1)

    # Выводим номера зараженных холодильников через пробел
    print(" ".join(map(str, infected_indices)))


def contains_anton(data):
    target = "anton"
    index = 0  # Индекс для отслеживания позиции в строке "anton"

    for char in data:
        if char == target[index]:  # Если символ совпадает с текущим символом из "anton"
            index += 1  # Переходим к следующему символу "anton"
        if index == len(target):  # Если все символы "anton" найдены
            return True

    return False  # Если не все символы найдены


# Запуск функции
find_infected_refrigerators()

# Второй способ решения задачи - взято из форума
for i in range(int(input())):
    s, virus, x = input(), 'anton', 0
    for sym in s:
        if sym == virus[x]:
            x += 1
        if x == 5:
            print(i + 1, end=' ')
            break
